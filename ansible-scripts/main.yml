---
  # - name: install docker and create user adolph
  #   sudo: yes
  #   hosts: base
  #   roles:
  #     - { role: init }

  - name: master
    sudo: yes
    hosts: master
    roles:
      - { role: master }
      
  - name: install docker images and setup mesos marathon env
    sudo: yes
    hosts: slaves
    roles:
      - { role: slaves }
